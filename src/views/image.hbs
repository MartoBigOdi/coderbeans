<div class="card">
    <div class="card-heaader d-flex justify-content-between align-items-center">
        <h2>{{image.title}}</h2>
        <button class="text-muted btn btn-outline-warning" id="btn-delete" data-id="{{image.uniqueId}}">
            <i class="fa fa-times"></i> Borrar</button>
    </div>
    </td>
    <div class="card-body">
        <p>{{image.description}}</p>
        <div class="text-center">
            <img src="/public/upload/{{image.filename}}" alt="" class="img-fluid">
        </div>
        <div class="card-heaader d-flex justify-content-between align-items-center">
        <button class="btn btn-outline-success mt-2" id="btn-like" data-id="{{image.filename}}">
            <i class="fas fa-thumbs-up"></i> Like
        </button>
        <p class="mt-4 btn-outline-success">
            <span class="likes-count"> {{image.likes}}</span>
            <i class="fas fa-heart"></i>
        </p>
        <p class="mt-4 btn-outline-success"> 
            {{image.views}} <i class="fas fa-eye"></i> 
        </p>
        <p class="mt-4 btn-outline-success"> 
            {{timeago image.timestamp}} 
        </p>
    </div>
</div>
</div>
<!--Comentarios-->
<div class="card mt-2">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h3>Comentarios</h3>
        <button class="btn btn-info" id="btn-toggle-comment"> 
            <i class="fa fa-comment-o"></i> Haz tu comentario
        </button>
    </div>
    <div class="card-body">
        <blockquote id="post-comment">
            <form action="/images/{{image.uniqueId}}/comment" method="POST">
            <div class="form-group">
                <input type="text" class="form-control" name="name" placeholder="Name" required/>
            </div>
            <div class="form-group">
                <input type="email" class="form-control" name="email" placeholder="Mail" required/>
            </div>
            <div class="form-group">
                <textarea name="comment" rows="2" class="form-control" placeholder="Tu comentario" required></textarea>
            </div> 
            <div class="form-group">
                <button class="btn btn-outline-success" id="btn-comment">
                    <i class="fa fa-comment"></i> Post
                </button>
            </div>  
            </form>
        </blockquote>
        <ul class="list-group-outline p-2">
            {{#each comments}}
                <li class="list-group-item">
                    <div class="row">
                        <a href="#" class="col text-center">
                            <img src="https://es.gravatar.com/avatar/{{gravatar}}?d=monsterid&s=45" alt="">
                        </a>
                        <blockquote class="col">
                            <p class="lead">{{comment}}</p>
                            <footer class="blockquote-footer">
                            {{name}} - <br> {{timeago timestamp}}
                            </footer>
                        </blockquote>
                    </div>
                </li>
            {{/each}}
        </ul>
    </div>
</div>